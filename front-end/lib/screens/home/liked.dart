import 'package:flutter/material.dart';

class UserLiked extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Thread Liked Screen"),
      ),

    );
  }
}
